spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vape_shop
    username: postgres
    password: sdbarakzai123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    database: postgresql
